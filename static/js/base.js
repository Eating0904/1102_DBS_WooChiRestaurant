window.onload = function() {
    // date picker
    $('.datepicker').datepicker({
        format: 'yyyy-mm-dd',
    });

    //固定頁面大小
    // $(".container").outerHeight($("html").outerHeight() - $("nav").outerHeight());

}